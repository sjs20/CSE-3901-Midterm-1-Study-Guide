1. $ vi a
   $ git add .
   $ git commit -m "a"
   $ vi b
   $ git add .
   $ git commit -m "b"
   $ vi c
   $ git add .
   $ git commit -m "c"
   $ vi d
   $ git add .
   $ git commit -m "d"
   $ vi e
   $ git add .
   $ git commit -m "e"
   $ git checkout <Commit b's Hash>
   $ vi f 
   $ git add .
   $ git commit -m "f"
   $ git branch maint
   $ git checkout main
   $ git merge maint

2.

3. 

4. 

5.

6. 

7. 

8. 

9. 

  
